<form method="post" action="{{ path('app_advert_delete', {'id': advert.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ advert.id) }}">
    <button class="btn">Delete</button>
</form>
