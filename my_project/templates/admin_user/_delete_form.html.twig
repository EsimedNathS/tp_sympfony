<form method="post" action="{{ path('app_admin_user_delete', {'id': admin_user.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin_user.id) }}">
    <button class="btn">Delete</button>
</form>
